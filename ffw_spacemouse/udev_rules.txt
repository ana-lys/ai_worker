# ==============================================================================
# SpaceMouse UDEV Rules (Physical Port Binding)
# ==============================================================================
# 1. Copy this file to your udev rules directory:
#      sudo cp src/ai_worker/ffw_spacemouse/udev_rules.txt /etc/udev/rules.d/99-spacemouse.rules
# 2. Reload the rules:
#      sudo udevadm control --reload-rules && sudo udevadm trigger
# ==============================================================================

# LEFT ARM (Plugged into port 1.4.4)
KERNEL=="js*", SUBSYSTEM=="input", ATTRS{phys}=="usb-0000:00:14.0-1.4.4/input0", SYMLINK+="input/spacemouse_left"

# RIGHT ARM (Plugged into port 1.1.2)
KERNEL=="js*", SUBSYSTEM=="input", ATTRS{phys}=="usb-0000:00:14.0-1.1.2/input0", SYMLINK+="input/spacemouse_right"
